@model IEnumerable<Pagos_colegio_web.Models.Estudiante>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Familia)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FechaInscripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Descuento)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tarifa)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Familia.NombreUsuario)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FechaInscripcion)
            </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Descuento)
                </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tarifa.Gestion)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.EstudianteId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.EstudianteId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.EstudianteId">Delete</a>
                    <a asp-controller="Reportes" asp-action="HistorialPagos" asp-route-id="@item.EstudianteId">HistorialPagos</a>
            </td>
        </tr>
}
    </tbody>
</table>
